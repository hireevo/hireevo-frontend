{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // The suite's own imports name their files, as the web app's do.
    "allowImportingTsExtensions": true,
    "types": ["node"],
    "tsBuildInfoFile": "./.turbo/tsconfig.tsbuildinfo"
  },
  "include": ["tests/**/*", "support/**/*", "playwright.config.ts"]
}
