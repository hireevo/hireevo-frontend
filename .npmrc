# Keep transitive deps out of the root node_modules so an app can only import
# what it actually declares. Undeclared imports must fail here, not in CI.
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false
engine-strict=true
